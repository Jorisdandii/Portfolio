import Sidebar from "../components/Sidebar";
import Pages from "../components/Pages";

const Root = () => {
  return (
    <div>
      <Sidebar />
      <Pages />
    </div>
  );
};

export default Root;
